<h1>My Images</h1>
<%= form_for(Image.new, html: { multipart: true, class: "dropzone"}) do |f|  %>
  <div class="fallback">
    <%= f.file_field :photo %><br>
    <%= f.submit "Upload my photo Avatar" %>
  </div>
<% end %>

<div class="index">
  <%= render "index" %>
</div>